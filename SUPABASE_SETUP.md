# CONFIGURACIÓN SUPABASE (FINAL) 🏁🐄

Para evitar errores, **no copies nada de esta pantalla**. 

### 🛠️ Pasos para el éxito:
1. Abrí el archivo [SUPABASE_SCRIPT.sql](file:///c:/Users/cesar/Desktop/Nueva%20carpeta/Nueva%20carpeta/tambo-app/SUPABASE_SCRIPT.sql).
2. Seleccioná todo el texto (**Ctrl + A**) y copialo (**Ctrl + C**).
3. Pegalo en el **SQL Editor** de Supabase y dale a **RUN**.

---
Una vez que termine, pasame la llave `anon` `public` de los **Settings -> API** y terminamos la configuración. 🚜💨✨⚖️✅
